var victimTotal = prompt("How many disaster victims are you entering?");

//var victimNumber = victimTotal * 1;
var victimInfo = function() {	
	var victimNumber = victimTotal * 1;
	for (var i = 0; i < victimNumber; i++) {
		var vicName = prompt("What is the victims name?");				
	}
};
victimInfo();
